# 更新日志

本项目所有值得记录的变更都会写在这里。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.修订号`。

## [1.1.0] - 2026-09-04

### 新增

- **模型过滤与搜索**
  - 工具栏下方新增搜索框，可按模型 ID 或来源实时过滤
  - 新增「只看可用」复选框，仅显示可用性测试通过的模型
  - 新增「清除」按钮一键重置过滤条件

- **首 token 延迟（TTFT）测速**
  - 测速改为流式请求（`stream=True`），新增「首token」列
  - 颜色分级：≤1s 绿色 / ≤2.5s 橙色 / 更慢红色
  - 接口不支持流式输出时自动回退为非流式测速（TTFT 显示 `-`）

- **模型价格 / 上下文 / 输出上限**
  - 新增价格（\$/1M tokens）、上下文、输出上限三列
  - 主数据源为 **OpenRouter**（单一权威定价），models.dev 自动补齐其未收录的模型
  - 获取模型列表后自动加载价格，无需手动点击
  - 大小写不敏感匹配，兼容 `DeepSeek-V4-Flash` / `deepseek-v4-flash` 等写法差异
  - 同名模型被多家服务商收录时取各字段中位数，避免取到偏离值

- **其他**
  - 界面加宽至 1280px，表格新增横向滚动条
  - 所有新列支持点击表头排序
  - 回调异常改为弹窗提示，不再静默失败

### 修复

- 价格按钮因重构后引用了已删除的变量而静默失效（点击无反应）
- 同名模型价格随机取到单一服务商数据，导致数值异常（如输出上限显示 1.0M）
- 大于等于 1M 的 token 数精度丢失（`1048560` 显示为 `1.0M`，现为 `1.05M`）

### 变更

- CSV 导出新增字段：`ttft_s`、`price_in_usd_per_1m`、`price_out_usd_per_1m`、`context_tokens`、`output_tokens`

## [1.0.0] - 2026-06-24

首个正式版本。

- 多 API 配置管理（本地保存，下拉切换，Key 掩码显示）
- 获取模型列表（ID / 创建日期 / 来源）
- 批量可用性测试（5 线程并发）
- 测速（tokens/s）与颜色分级
- CSV 导出
- 点击表头排序
